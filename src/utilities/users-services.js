// import * as usersAPI from "./users-api";

// // this add the user to the database

// export async function signUp(userData) {
//   //delegate the network request to the userr-api api module
//   //which will ultimately return a json web token

//   const token = await usersAPI.signUp(userData);
//   console.log(token);
//   //local storage to store the token
//   //localStorage.setItem('token', token);
//   return { name: userData.name, email: userData.email };
// }
